<html>
    <head>
        <meta charset="utf-8"</meta>
    </head>
    <body>

        <%= nav_bar brand:"Produtos", brand_link:root_url %>

        <table class="table table-bordered">
            <thead>
                <th>Produtos</th>
                <th>Descricao</th>
                <th>Preço</th>
                <th>Quantidade</th>
            </thead>
            <tbody>
                <% @listaProduto.each do |produto| %>
                <tr>
                    <td><%= produto.nome %></td>
                    <td><%= produto.descricao %></td>
                    <td><%= produto.preco %></td>
                    <td><%= produto.quantidade_estoque %></td>
                </tr>
                <% end %>
            </tbody>
        </table>
</body>
</html>